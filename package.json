{
  "name": "@buggyorg/library-specification",
  "version": "0.1.0",
  "description": "Specification for buggy component library servers.",
  "main": "lib/index.js",
  "scripts": {
    "test": "standard src/**/*.js",
    "build": "babel src -d lib && swagger-to-md -y spec/swagger.yaml > spec/api.md",
    "prepublish": "babel src -d lib && swagger-to-md -y spec/swagger.yaml > spec/api.md"
  },
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.13.1",
    "chai": "^3.5.0",
    "chai-as-promised": "^5.3.0",
    "chai-http": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-preset-es2015": "^6.6.0",
    "babel-register": "^6.7.2",
    "mocha": "^2.4.5",
    "standard": "^6.0.8",
    "swagger-to-md": "^1.0.0"
  }
}
